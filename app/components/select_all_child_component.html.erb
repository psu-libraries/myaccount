<%= check_box_tag "#{name}[#{key}]",
                  key,
                  false,
                  data: { select_all_target: target_keyword },
                  class: 'checkbox',
                  multiple: true,
                  name: name %>
