<p>Volume(s) Selected -- if you need specific volume(s) (e.g., vol.1 and/or vol.2) check each desired volume </p>
<% items.each do |item| %>
  <%= render PlaceHoldCheckboxComponent, barcode: item.barcode,
                                         call_number: item.call_number,
                                         pickup_library: item.pickup_library_human %>  <br>
<% end %>
